type: logic_ternary
message0: if %1 then %2 else %3
args0:
  - type: input_value
    name: IF
    check: Boolean
  - type: input_value
    name: THEN
  - type: input_value
    name: ELSE
output: null
colour: 210
tooltip: Returns one of two values based on a condition
help_url: >-
  https://github.com/google/blockly/wiki/Logic#ternary-operator-questionmark
category: Logic
